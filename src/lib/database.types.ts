/**
 * Type générique pour la base de données Supabase
 * Mode DEMO: Type minimal pour éviter les erreurs de compilation
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {
  // Tables définies dans Supabase
  // En mode DEMO, ce type n'est pas utilisé car on utilise les données mock
}
